# ledgercaddy.com

Marketing site for LedgerCaddy (DART Management Inc.) — served by GitHub Pages.
Staging until launch: served at the github.io URL only; the custom domain connects at launch.
`index.html` is the whole site; edit and push to publish.
